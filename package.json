{
  "name": "socketer",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "serve": "bun run ./src/serve.ts",
    "compile": "bun run ./src/compile.ts",
    "commit": "bun run ./src/commit.ts",
    "watch": "bun build src/extension.ts src/overlay.ts --minify --outdir=dist --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/firefox-webext-browser": "^120.0.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}